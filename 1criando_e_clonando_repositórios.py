//cd.. volta uma pasta
  
// transformando um diretório local que não está sob controle de versão, num repósitório git:

git bash here // vai até o local da pasta ou mkdir(cria nv pasta) e cd nomeDaPasta

git init //inicializa repositório

git remote add origin https //conecta o repositorio local ao repositorio remoto que eu quiser no GitHub
  
  //clonando um repósitório git existente :

// 1 °vai na pasta que voce deseja

git clone https(GitHub)

git remote -V //para ver se está conectado mesmo
